[project]
name = "wobl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "control>=0.10.2",
    "dm-control==1.0.31",
    "mujoco==3.3.5",
    "numpy>=2.3.5",
    "pynput>=1.7.6",
    "pyserial>=3.5",
    "rerun-sdk>=0.27.2",
    "scipy>=1.11.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
